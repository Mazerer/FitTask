name: Fittask
description: "Mazer's Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter_time_picker_spinner: ^2.0.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  uuid: ^4.5.1        
  intl: ^0.20.0      
  shared_preferences: ^2.2.3  
  cupertino_icons: ^1.0.8
  flutter_local_notifications: ^19.4.1
  timezone: ^0.10.1
  device_info_plus: ^12.3.0
  workmanager: ^0.9.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true