name: fittask
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  uuid: ^3.0.7        # Последняя версия на момент моего знания
  intl: ^0.18.1       # Последняя версия на момент моего знания
  shared_preferences: ^2.2.3  # Последняя версия на момент моего знания
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true